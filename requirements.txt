[project]
name = "aternos-discord-bot"
version = "0.1.0"
description = "A Discord bot that starts an Aternos server with the !start command."
requires-python = ">=3.11"
dependencies = [
    "discord-py>=2.7.1",
    "flask>=3.1.3",
    "python-aternos>=3.0.6",
]

[project.scripts]
aternos-discord-bot = "main:main"
